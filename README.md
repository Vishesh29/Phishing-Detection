# Phishing-Detection
A complete MLOps pipeline for detecting phishing URLs using machine learning.   Includes data ingestion from MongoDB, validation, transformation, model training, and a Flask web interface for predictions.
